# Axon Project Root Bazel Configuration

# Import modular configurations
import %workspace%/build_deps/bazelrc/cc.bazelrc
import %workspace%/build_deps/bazelrc/cuda.bazelrc
import %workspace%/build_deps/bazelrc/python.bazelrc
import %workspace%/build_deps/bazelrc/profiles.bazelrc

# User-specific local overrides (should not be checked into version control)
try-import %workspace%/.local_config.bazelrc
